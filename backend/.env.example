JWT_SECRET="<your-jwt-secret>"
DATABASE_URL="file:./dev.db"

# DEV ONLY: Enable seed data mutation (set to "true" to enable)
# Hard guard: Always disabled in production regardless of this value
DEV_SEED_ENABLED="true"
